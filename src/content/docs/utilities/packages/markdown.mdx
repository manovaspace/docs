---
title: "@manovaspace/markdown"
description: Lightweight GFM markdown renderer for React and Next.js.
---

Renders GitHub-Flavored Markdown with typography utility classes. Bring your own Tailwind theme — no CSS bundle ships with the package.

## Install

```bash
pnpm add @manovaspace/markdown
```

Peers: `react`, `react-dom`. Also works with npm or yarn.

## Usage

```tsx
import { MarkdownContent } from "@manovaspace/markdown";

export function BlogPost({ source }: { source: string }) {
  return <MarkdownContent>{source}</MarkdownContent>;
}
```

Works in Server and Client Components.

## API

| Export | Description |
| --- | --- |
| `MarkdownContent` | Renders markdown string children with GFM |
| `MarkdownContentProps` | `{ children: string }` |

## Related

- [Utilities overview](/docs/utilities/)
- [Getting started](/docs/utilities/getting-started/)
- [npm](https://www.npmjs.com/package/@manovaspace/markdown)
- [Source](https://github.com/manovaspace/ts/tree/main/packages/markdown)
